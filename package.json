{
  "private": true,
  "workspaces": {
    "packages": [
      "covfee/client",
      "covfee/shared"
    ],
    "nohoist": [
      "docs/**"
    ]
  },
  "scripts": {
    "ppp": "rm -rf dist/ && covfee-dev schemata && covfee-dev build && python -m build && python -m twine upload dist/*"
  }
}
