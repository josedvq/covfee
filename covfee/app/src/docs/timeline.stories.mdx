import { Description } from '@storybook/addon-docs/blocks'
import marked from 'marked'
import {updateMarkdownLinks} from '../docs'

<Meta title="Timeline HITs"/>

<Description>{updateMarkdownLinks(marked(timeline))}</Description>