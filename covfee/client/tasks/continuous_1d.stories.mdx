import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import Continuous1DTask from './continuous_1d'
import schemata from '@server/schemata.json'
import {TaskForm} from '@docs/utils/task_form'

<Meta title="Tasks / Continuous 1D" component={Continuous1DTask} />

export const Template = (args) => <TaskVisualizer {...args } />

# Continuous annotation of a 1D variable

This component was designed to capture the continuous annotation of a single one-dimensional variable, like arousal or valence in annotations of emotional expresion. The user controls the annotation interface via mouse or keyboard.

export const taskSpec = {
    "name": "Continuous annotation example",
    "type": "Continuous1DTask",
    "media": {
        "type": "video",
        "url": "https://file-examples-com.github.io/uploads/2017/04/file_example_MP4_480_1_5MG.mp4",
        "mute": true,
    },
    "intensityInput": {
        "mode": "gravity-keyboard",
        "jump_speed": 0.1,
        "acceleration_constant": 0.008,
        "controls": {
            "up": "a"
        }
    }
}

export const uiSchema = {
    'ui:order': ['name', 'type', 'intensityInput', 'media','controls',],
    type: {'ui:widget':'hidden'},
    intensityInput: {
        mode: {'ui:widget':'hidden'}
    }
}

<TaskForm spec={taskSpec} 
    schema={schemata['definitions']['Continuous1DTaskSpec']}
    uiSchema={uiSchema}/>

## Component Props (for developers)

<ArgsTable of={Continuous1DTask}/>